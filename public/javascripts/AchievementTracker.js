class AchievementTracker{
    /**
     *
     * @param curMissionCnt
     * @param curDistance
     * @param curLabelsCnt
     * @param curValidationsCnt
     * @param curAccuracy
     */
    constructor(curMissionCnt, curDistance, curLabelsCnt, curValidationsCnt, curAccuracy){
        this.mapBadges = {
            "missions" : {
                1 : new Badge("mission", )
            }
        }
    }

    // images/badges/Badge_Missions_Badge1.png
}

class Badge{
    /**
     *
     * @param name
     * @param iconPath
     * @param badgeAwardThreshold
     */
    constructor(name, iconFilename, badgeAwardThreshold){

    }
}