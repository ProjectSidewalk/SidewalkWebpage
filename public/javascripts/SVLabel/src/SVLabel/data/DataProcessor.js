/**
 * Todo. Take the data processing/cleaning parts in the Form object and tack them in here.
 * @param dataModel
 * @constructor
 */
function DataProcessor (dataModel) {
    var self = this;
    var _dataModel = dataModel;
}

