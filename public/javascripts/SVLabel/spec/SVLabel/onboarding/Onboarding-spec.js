describe("Tests for the Onboarding module.", function () {
    var onboarding;
    var statusModelMock;

    beforeEach(function () {
        statusModelMock = _.clone(Backbone.Events);
        onboarding = Onboarding(statusModelMock);
    });

    describe("_init method", function () {
        beforeEach(function () {


        });
    });
});
