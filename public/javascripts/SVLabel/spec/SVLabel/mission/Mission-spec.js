describe("Mission module", function () {
    var mission;

    beforeEach(function () {

    });

    describe("`adjustTheTargetDistance` method", function () {
        it("should adjust the target distance if the total distance of available tasks are less than the target audit distance");
    })
});