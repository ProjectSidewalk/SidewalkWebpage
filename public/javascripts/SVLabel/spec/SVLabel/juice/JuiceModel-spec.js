describe("Keyboard module.", function () {
    var model;

    beforeEach(function () {
        model = new JuiceModel();
    });

    describe("`play` method", function() {
        beforeEach(function () {
            spyOn(model, 'play');
            model.play();
        });

        it("", function() {
            spyOn(model, 'play');
            model
        });
    });
});
