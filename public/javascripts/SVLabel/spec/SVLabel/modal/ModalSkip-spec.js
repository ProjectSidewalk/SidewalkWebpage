describe("ModalSkip module", function () {
    var modalSkip;
    var modalModel;

    beforeEach(function () {
        modalModel = _.clone(Backbone.Events);
        modalSkip = new ModalSkip(modalModel);
    });

});