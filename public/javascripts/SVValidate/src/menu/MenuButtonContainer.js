/**
 * Holds all instances of MenuButtons to support validation interfaces that contain
 * more than one panorama per screen.
 * @param buttonMap                 An object that contains a mapping of panorama id names to
 *                                  jQuery menu button groupings.
 * @returns {MenuButtonContainer}
 * @constructor
 */
function MenuButtonContainer (buttonMap) {
    var self = this;

    function _init() {
        buttonMap.forEach(function(panoramaId) {

        });
    }

    _init();

    return this;
}