root = true

[*]
end_of_line = lf
insert_final_newline = true
indent_style = space
trim_trailing_whitespace = true
# indent_size = 2 # Once we're ready to enforce formatting on non-Scala files, we can uncomment this line.

# If any settings above are being applied too broadly, we can add them below [*.scala] to just apply to Scala files.
# [*.scala]

# TODO we want to reformat all files to use 2 spaces. It's set to 4 until we can reformat all files.
[{*.json,*.js,*.html}]
indent_size = 4
