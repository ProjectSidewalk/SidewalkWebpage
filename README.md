# Issue assets

Screenshots referenced from issues and pull requests. This branch is **not** part of the codebase and is never
merged into `develop` — it exists only so images have a stable URL. Files are named `<issue>-<description>`.
