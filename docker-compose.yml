version: '3.8'

services:

  web:
    image: projectsidewalk/web
    container_name: projectsidewalk-web
    build:
      context: ./
    shm_size: '256mb'
    depends_on:
      - db
    volumes:
      - "./:/opt/:delegated"
      - "/opt/node_modules"
    ports:
      - "9000:9000"
    platform: linux/x86_64
    # platform: linux/arm64
    environment:
      - DATABASE_URL=jdbc:postgresql://db:5432/sidewalk-pittsburgh
      - SIDEWALK_CITY_ID=pittsburgh-pa
      - SIDEWALK_EMAIL_ADDRESS=DUMMY_EMAIL_ADDRESS
      - SIDEWALK_EMAIL_PASSWORD=DUMMY_EMAIL_PASSWORD
      - MAPBOX_API_KEY=pk.eyJ1IjoicHJvamVjdHNpZGV3YWxrIiwiYSI6ImNsZzE0ODJwOTFrbzIzZm9hb3pkZmZ0bHkifQ.-sRpTTdoXd4JNgT3vFbODw
      - GOOGLE_MAPS_API_KEY=AIzaSyAzDsZNcL6MFGza58UFuf3iMeJln3hyfRQ
      - GOOGLE_MAPS_SECRET=MYvWOAYu0J6ZtJ9TRWg6ETRWuAQ=
      - SCISTARTER_API_KEY=DUMMY_SCISTARTER_API_KEY
      - INTERNAL_API_KEY=DUMMY_INTERNAL_API_KEY
      - ENV_TYPE=test

  db:
    image: projectsidewalk/db
    container_name: projectsidewalk-db
    build:
      context: ./db/
    shm_size: '256mb'
    volumes:
      - "./db/init.sh:/docker-entrypoint-initdb.d/init.sh"
      - "./db:/opt"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=sidewalk
