# Dependencies for the offline tooling in tools/city/ — check_streets_for_imagery.py and onboard_city.py (see
# tools/city/README.md). Kept out of requirements.txt, which is pinned to what the app's in-band Python (3.8) can install.
#
# These need Python >= 3.11 (pandas sets the floor), so they install into the container's second interpreter:
#
#   python3.13 -m pip install -r requirements-offline-tools.txt
#
# Self-contained rather than layered on requirements.txt: the two target different interpreters, so no shared pin.
pandas==3.0.5
requests==2.34.2
shapely==2.1.2
geopy==2.5.0
tenacity==9.1.4
tqdm==4.70.1

# onboard_city.py's geo stack.
osmnx==2.1.1
geopandas==1.1.4
pyogrio==0.13.0
# osmnx imports scipy unconditionally; pinned so pip can't resolve to a build mismatched with the numpy it picks.
scipy==1.17.1

# street_gradient.py's raster stack. The rasterio wheel bundles its own GDAL, so nothing is needed from the OS. pyproj
# already arrives with geopandas and is pinned here because the script imports it directly.
rasterio==1.5.1
pyproj==3.8.0
